@page "/save-product"
@page "/save-product/{Sno:long}"
@attribute [Authorize(Roles = "Admin,User,ManageProduct")]

<AstroOfficeSharedComponents.SaveProduct Sno="Sno"></AstroOfficeSharedComponents.SaveProduct>

@code{
    [Parameter]
    public long Sno { get; set; }
}